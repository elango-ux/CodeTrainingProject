#user error => type error or index error
numbers = [1, 2]
# it through type error or index error because number does not exist
#print(numbers[3])
# value error  invalid literal of int() with base a
print (int(input("Age:")))