#first letterof every word is capital
class point:
    # function is defined inside class
    def draw(self):
        print("draw")
    #create object
    point = point()
    #to find point object is instanace of class point
    print(isinstance(point, point))